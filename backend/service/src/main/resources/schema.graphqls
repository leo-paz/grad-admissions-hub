type Query {
  professorById(id: ID): Professor
  applicantById(id: ID): Applicant
}

type Professor {
  id: ID
  name: String
  areaOfResearch: String
}

type Applicant {
  id: ID
  name: String
  areaOfResearch: String
}