import React from 'react'

import Accordion from '../Accordion/Accordion';

function Review({title, body, rating, date}) {

    return (
        <Accordion
            title={title}
        >
            <h1>hello</h1>
        </Accordion>
    )
}

export default Review;